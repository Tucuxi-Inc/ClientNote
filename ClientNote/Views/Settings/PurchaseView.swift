// This file has been removed for the free version
// All subscription functionality is disabled
import SwiftUI

struct PurchaseView: View {
    var body: some View {
        VStack {
            Text("Subscription features removed")
                .font(.title)
            Text("This is now a free app")
                .font(.subheadline)
        }
        .padding()
    }
}